import * as types from './favoritesTypes';

const initialState = {
    list: [],
}

export default appReducer = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}